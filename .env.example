# Guava Admin Dashboard — environment variables
# Copy to .env.local and fill in values.

# Backend API base URL (no trailing slash).
# When blank the dashboard runs entirely on built-in mock data.
VITE_API_BASE_URL=https://api.guava.app
